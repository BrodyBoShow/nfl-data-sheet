{
  "permissions": {
    "deny": [
      "Read(./data/**)",
      "Read(./.cache/**)",
      "Read(**/.cache/**)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import sys,json; print(json.load(sys.stdin).get('tool_input',{}).get('file_path',''))\" | { read -r f; case \"$f\" in *.py) cd \"C:/Users/Brody/NFL_Agent_Data_Sheet\" && uv run ruff format \"$f\" && uv run ruff check --fix \"$f\" ;; esac; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
