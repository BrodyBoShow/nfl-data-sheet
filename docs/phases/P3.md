# P3 — Availability

**Status:** not started (blocked on P2)

> **Scope correction (2026-09-17):** Usage and role moved back to P7 (P2 is Efficiency
> only now), so it's no longer available by this phase. Availability impact reverts to
> the original arrangement: raw snap shares as the redistribution baseline, not Usage's
> target/carry shares. See `docs/architecture.md` §Deviations.

## Scope
- Availability collector: ESPN injuries, Sleeper players (max once/day), NFL.com injury
  report pages. Stores injuries, practice_status, transactions.
- Availability impact analyst: target/carry redistribution, replacement quality gap,
  OL/secondary cluster flags, practice-trend risk. Consumes raw snap shares (from the
  `snaps` table, populated by the Phase 2 nflverse bulk collector) as the redistribution
  baseline, since Usage/role's more refined target/carry shares aren't built until P7.
  Revisit whether to switch to Usage's shares once P7 exists.
- Source verification: one live call per endpoint, fixture saved, `docs/sources.md`
  updated from UNVERIFIED to VERIFIED with real shapes/limits.

## Files to create
```
db/migrations/0008_availability.sql   # injuries, practice_status, transactions
                                       # (0006 taken by staged_nflverse in P2,
                                       # 0007 by enable_rls)
pipeline/collectors/availability.py
pipeline/analysts/availability_impact.py
tests/fixtures/espn_injuries_sample.json
tests/fixtures/sleeper_players_sample.json
tests/fixtures/nflcom_injury_report_sample.html (trimmed)
tests/test_availability.py
tests/test_availability_impact.py
```

## Done when
- Current week's statuses stored with verified sources.
- Impact signals computed for teams with key absences, using raw snap shares as the
  redistribution baseline.
- `docs/sources.md` entries for these sources moved to VERIFIED.
