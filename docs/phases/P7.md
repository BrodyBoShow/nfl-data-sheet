# P7 — Usage/role, player efficiency, scheme, and intel

**Status:** in progress. Step 1 (docs) done 2026-09-25; step 2 (verify + fixtures) next.
The web player view is **blocked** on the PFR/NGS license quotes (open item 2).

> **Scope correction (2026-09-17):** Usage and role moves back here from P2 — P2 is
> Efficiency only now. Matchup history (previously slated to join Usage in P2) is cut
> from the plan entirely, not rehomed here either.

> **Scope split (2026-09-22, ahead of P5 locking synthesizer inputs):** player-level
> efficiency (EPA/target, success rate, YAC-oe, separation, rush-by-gap, etc.) splits out
> of Usage/role into its own analyst — Usage answers "how much does this player do,"
> player-efficiency answers "how well," and combining them would need "and" to describe
> in one docstring, which CLAUDE.md's agent-scoping rule treats as a signal to split
> before writing code. P7 is now a three-analyst phase: Usage/role, Player efficiency,
> Scheme (Intel unchanged).

> **Storage redesign (2026-09-25):** player detail goes into wide per-analyst player
> tables, not `signals`. Team-level signals stay in `signals`. Participation joins as a
> historical-prior source. An L0 retention job is added. The research, arithmetic, and
> decisions are below.

## Scope
- **Usage and role analyst** (`usage.py`): snap share (offense, defense, ST, for every
  player who takes a snap), target share, air-yards share, red-zone/goal-line/end-zone
  share, carry share, week-over-week deltas. Reads `player_week`/`snaps`. Writes
  `player_usage_week`.
- **Player efficiency analyst** (`player_efficiency.py`): per-player rate stats for
  receiving, rushing, passing, and defense. It's the individual-side counterpart to P2's
  team-level Efficiency analyst. Reads `player_week`, `ngs`, `pfr_advstats`, `snaps`,
  and the new `player_game_pbp` and `participation_player_season` staged tables. Writes
  `player_eff_week`.
- **Scheme analyst** (`scheme.py`), team-level, writes `signals`:
  - pass rate over expected, neutral pace
  - play-action/motion/RPO/screen rates, box counts, blitz/pressure rate
  - approximate personnel (labeled as approximate)
  - run defense by gap/direction, PFR nearest-defender coverage-proxy splits
  - participation-derived man/zone and coverage-shell tendencies, labeled as
    multi-season history (see "Participation labeling rule")
- **nflverse bulk collector, extended** (not a new collector: participation is the same
  nflverse-data release family, one collector per source family):
  - Widened `ngs`/`pfr_advstats`/`ftn` columns.
  - A player-keyed pbp+FTN count aggregation (`player_game_pbp`).
  - A participation player-season aggregation (`participation_player_season`, tag
    `pbp_participation`), gated by that tag's own `timestamp.json`.
- **Retention job** (L0, `pipeline/orchestration/retention.py`): the decided policies
  L2 and L4 below.
- **Intel collector**: ESPN NFL news feed, official team RSS where available, Sleeper
  trending players as a news-spike indicator. Dedupe by URL + text hash. Rule-based
  tagging only (keywords + crosswalk name matching): team, player_id, category (injury,
  depth chart, suspension, coaching, signing/release/trade). No LLM calls.

## Decisions (user, 2026-09-25)
1. **Shape:** wide per-analyst player tables. **Team-level signals stay in `signals`.**
   The single-shape contract still holds for everything the model reads; the exception
   is player detail only (CLAUDE.md layer rules, `docs/signals.md` "Player tables").
2. **Detail:** the full version. Every metric stores season-to-date (STD, prior-blended),
   this game, and last-4 games. The last-4 values answer "is this player trending."
3. **Retention:**
   - **L2 yes:** staged nflverse player tables keep only `[season-1, season]`.
   - **L4 yes:** after a season ends, its weekly player-table rows collapse to each
     player's final row.
   - **L5 deferred:** the `signals.inputs_version` lookup table.
   - **L3 not decided:** trimming `injuries.raw`. First find out whether the changelog's
     ~450 rows/day are real changes or hash churn (open item 1).
4. **Archive** (export completed seasons to Parquet, delete from Postgres): deferred.
   It's the plan for before the 2028 offseason; revisit then.
5. **Participation priors:** included, with CC-BY-SA 4.0 attribution, the same as FTN.

## Open items
1. **Injuries changelog churn (decides L3).** `injuries` is the largest growth source:
   948 B/row, 566 B of it the `raw` jsonb, ~450 rows/day (measured 2026-09-18..25).
   - Does `content_hash` cover a volatile field? Candidates: a timestamp inside `raw`,
     notes text reformatting, or ordering inside arrays.
   - If so, fixing the hash saves more than truncating payloads, and loses nothing.
   - Method: for players with >1 row in a day, diff consecutive rows field by field,
     and count rows where only `raw` or `as_of`-like fields differ. Use the collector's
     own `hash_row` input construction, not hand-written SQL (CLAUDE.md verification
     rule).
2. **PFR and NGS license (blocks the web player view, not the collectors).** nflverse
   states CC-BY-SA only for FTN and participation. For `snaps`/`pfr_advstats` (Pro
   Football Reference) and `ngs` (NFL Next Gen Stats) it states nothing.
   - Resolve it the same way as the Odds API: quote the actual clauses with URL and date
     in `docs/sources.md`, bring them to the user, don't interpret.
3. **`inputs_version` in the player tables: decided (user, 2026-09-25), plain text**,
   the same as `signals`.
   - An int id here, while `signals` still uses text, would give one field two
     conventions. If L5 ever happens, it changes both together.
   - Cost: ~6 MB on the current season's standing player detail (19 → ~25 MB). Under L4,
     past seasons cost ~2 MB either way. The arithmetic below still shows the 4-byte-id
     figure (19 MB).
4. **Archive home** (deferred with the archive): GitHub Release asset limits, repo
   visibility, and terms are unverified. Quote them before relying on them.

## Source inventory (research pass, 2026-09-25)
Measured on 2025 staged data and live nflverse releases. Full shapes and license quotes:
`docs/sources.md`.

| Source (loader) | Per-player fields | Raw vs rate | Seasons | Completeness | License |
|---|---|---|---|---|---|
| `player_week` (`load_player_stats`; 32 of 150 cols staged) | pass/rush/rec counts, yards, TDs, air yards, YAC, EPA sums, first downs, 10+/20+/40+ play counts, `target_share`, `air_yards_share`, `wopr`, `racr`, `pacr`, `passing_cpoe`; def box score (tackles, TFL, sacks, QB hits, PD, INT, FF) | counts/sums, plus share/ratio columns | 1999+ | every player with a stat line, ~1,029 rows/wk | CC-BY 4.0 (nflverse) |
| `snaps` (`load_snap_counts`) | off/def/ST snaps + pct, position | counts + pct | 2012+ | 99.8% crosswalk-resolved | nflverse; PFR terms pending (open item 2) |
| `ngs` (`load_nextgen_stats` ×3; 8 of ~40 cols staged) | **pass:** `avg_time_to_throw`, aggressiveness, air-yards diff/to-sticks, intended/completed air, max air distance, CPOE/xComp%. **rush:** RYOE (+/att, pct), efficiency, `percent_attempts_gte_eight_defenders`, `avg_time_to_los`. **rec:** `avg_cushion`, `avg_separation`, intended-air share, YAC/xYAC/YAC-oe, catch% | averages/rates, plus attempt counts | 2016+ | **thresholded**: ~30 QB, ~32 RB, ~67 receiver rows/wk | NFL NGS; terms pending (open item 2) |
| `pfr_advstats` (×4; 14 of ~70 cols staged) | **pass:** drops, bad throws, pressured/blitzed/hurried/hit (+pct). **rush:** yds before/after contact (+avg), broken tackles. **rec:** drops, broken tackles, INT when targeted, rating when targeted. **def:** targets, cmp, cmp%, yds, yds/cmp, yds/tgt, TD, rating allowed, aDOT allowed, air yds, YAC allowed, blitzes, hurries, hits, sacks, pressures, tackles, missed tackles, missed tackle % | counts + pcts/avgs | weekly 2018+ (not re-verified) | 99.9%+ resolved; def ~420 rows/wk; **no position column** | PFR terms pending (open item 2) |
| `ftn` (`load_ftn_charting`; 9 of 29 cols staged) | Play-level, so per-player only via a pbp join on passer/receiver/rusher. **Unstaged:** `read_thrown`, `is_catchable_ball`, `is_contested_ball`, `is_created_reception`, `is_drop`, `is_interception_worthy`, `is_throw_away`, `is_qb_out_of_pocket`, `is_qb_fault_sack`, `qb_location`, `starting_hash`, `is_trick_play`, `is_qb_sneak` | raw play flags | 2022+, charted within 48h | ~47k plays/season | CC-BY-SA 4.0, "FTN Data via nflverse" |
| participation (`load_participation`, new) | Per play: 22 on-field gsis IDs (+positions 2023+), personnel, formation, box, pass rushers, **man/zone**, **coverage shell**, **primary receiver's route**, `was_pressure`, `time_to_throw` | raw play-level | **2016–2025, post-season release only** | 2025: man/zone + shell 49% of plays, route 42%, TTT 43%. 2022: ~38% | CC-BY-SA 4.0, "FTN Data via nflverse" (2023+) / "NFL NextGenStats via nflverse" (≤2022) |
| `depth` | depth rank per slot | ordinal | current, latest only | — | nflverse |
| pbp (never stored raw) | `run_location` × `run_gap` (7 cells), `pass_location`, `success`, EPA; defender credits (`pass_defense_1/2_player_id`, `solo/assist_tackle_1_player_id`, `interception_player_id`, `qb_hit_1_player_id`, sack IDs) | play-level; aggregate only | 1999+ | fixture game: PD on 13 of 81 pass plays, tackler on 51 | CC-BY 4.0 |
| `load_ff_opportunity` (not used) | expected vs. actual yards/TD/first downs per player-week | third-party model outputs, not observations | not checked | 6,054 rows (2025) | ffverse, not read |
| `load_rosters_weekly` (not used) | weekly status, `depth_chart_position`, `ngs_position` | labels | not checked | 46,849 rows (2025) | nflverse |

Not useful here:
- `load_injuries`: dead after 2024.
- `load_officials`: game-level only.
- `load_trades`.
- `load_ff_rankings`: expert-opinion rankings, not performance.
- `load_combine`/`load_draft_picks`/`load_contracts`: static, one row per player. Out of
  scope this phase.

**Corrections to earlier assumptions:**
- `pfr_advstats` carries **no time to throw and no offensive air yards**. TTT is NGS
  passing and participation. PFR `def` carries air yards/YAC *allowed*.
- Participation did not stop. It's NGS-sourced through 2022 and FTN-sourced from 2023,
  but only after the season, so no 2026 data exists in-season.

### Per-week sample size (2025 REG medians; starter ≥70% snaps, rotational 25–70%)
| Pos | Starter | Rotational | NGS row present (starter/rot) |
|---|---|---|---|
| QB | 34 dropbacks | 13 dropbacks | 99% / 38% |
| RB | 17 carries, 4 targets | 9 carries, 2 targets | 94% / 47% |
| WR | 6 targets | 3 targets | 70% / 25% |
| TE | 5 targets | 2 targets | 60% / 13% |
| CB | 5 PFR targets (p90 8) | 3 (p90 6) | — |
| S | 3 targets | 2 | — |
| LB | 3 targets, 0 pressures (p90 2) | 1 target | — |
| DL | 1 pressure (p90 4) | 1 pressure (p90 3) | — |

FTN-derived per-player fields inherit these denominators. Rush-by-gap spreads 17
carries over 7 cells (~2.4 per cell). **Only QB dropback metrics carry a usable
single-game sample.** Per-game and last-4 values are shown for trend and context, always
next to `stability`. The STD prior-blended value is the one to trust.

### Coverage: who covered whom
**No free source says which defender covered which receiver on a snap.** That takes
tracking-derived matchup charting (PFF, SIS, NGS's non-public matchup product). The
closest proxies, and how each differs:
1. **PFR `def_targets`/allowed stats** (best in-season proxy): per defender per game,
   attributed by PFR's charting to the defender judged responsible on *targeted* plays.
   - No untargeted coverage snaps, so no real denominator.
   - No receiver identity: these are game aggregates.
   - Charting judgment, not tracking.
2. **pbp defender credits** (`pass_defense_*`, tackler, INT): play-level, and on the
   same row as `receiver_player_id`, so it is a real receiver↔defender link. But it only
   exists when that defender broke up, intercepted, or tackled, and a tackle isn't
   coverage. It's biased toward completions and PDs.
3. **Participation** (historical only): on the field isn't covering. It supports
   receiver-vs-man/zone/shell splits, and an on-field-dropbacks denominator for PFR
   targets, as priors only.
4. **NGS `avg_cushion`/`avg_separation`**: distance to the *nearest* defender, identity
   unknown, thresholded.

Any signal built on these is documented as what it is, never as a matchup or a
PFF-style grade.

### Participation labeling rule (decision carry-forward A)
Participation is 2016–2025, historical only, with man/zone on ~49% of plays. **Anything
derived from it is a multi-season tendency, not current-season behavior.**
- Signal and column names end in `_hist`.
- `inputs_version` names the seasons used (e.g. `pbp_participation@<ts>[2023-2025]`),
  and the registry entry states the span.
- The UI shows the span next to the value ("38% man, 2023–25"). "This defense plays 38%
  man" must never read as "this year."

## Storage design (research pass, 2026-09-25)

### Measured baseline
- `pg_database_size` 204.6 MB (195 MiB). `signals` is 74.5 MB of it (50.7 heap + 23.8
  index), 164,596 rows = **453 B/row**. `inputs_version` is 137 chars on efficiency
  rows; the unique index is 93 B/row.
- All-in growth today is **~139 MB/season**, not 27:
  - signals ~27
  - staged nflverse player tables ~29 (`ftn` 10.0, `player_week` 7.0, `snaps` 6.5,
    `pfr_advstats` 4.3, `ngs` 0.7). These never roll off: the collector fetches
    `[season-1, season]`, and nothing deletes older seasons.
  - `injuries` ~75 (open item 1; assumes ~168 in-season collection days)
  - other ~8
- Players who take ≥1 off/def snap in a 2025 week averaged **1,143**. The
  season-to-date "seen" population averaged **1,680** (1,166 in week 1, 1,965 by week
  18), a ratio of 1.47.
- Free plan: read-only at 500 MB *database size* (Supabase docs, "Free Plan behavior",
  read 2026-09-25). Pro: $25/mo, 8 GB (supabase.com/pricing, 2026-09-25).

### Row-cost model
- **`signals` row:** 453 B, measured.
- **Wide row (estimate; step 7 measures it):**
  - ~130 B fixed (keys, `as_of`, a 4-byte `inputs_version` id, `content_hash`), plus
    6 B per non-null metric value (4 B `real` + 2 B `smallint` league pct).
  - Multiply by 1.2 for page overhead and upsert churn, then add ~56 B of indexes.
  - A null costs 1 bit.
- **Season = 19 REG-week equivalents** (18 + postseason).

### Per signal group (every player who takes a snap, every free metric)
| Group | Played/wk | Metrics (typ.) | In `signals`, STD snapshot for all seen (rows/wk → MB/season) | In `signals`, played-only | **Wide as-of (rows/wk → MB/season)** |
|---|---|---|---|---|---|
| Usage, skill | 367 | 10 | 5,400 → 46.5 | 31.6 | 367 → 2.1 |
| Usage, DEF/OL snap + ST share | 776 | 2 | 2,280 → 19.6 | 13.3 | 776 → 3.4 |
| Receiving (~32 metrics) | 240 | 25 | 8,825 → 76.0 | 51.7 | 205 → 1.6 |
| Rushing (~42, incl. 7 gap cells × share/EPA/SR) | 125 | 30 | 5,520 → 47.5 | 32.3 | 95 → 1.1 |
| Passing (~42) | 37 | 38 | 2,052 → 17.7 | 12.0 | 37 → 0.4 |
| Defense (~26) | 420 | 24 | 14,808 → 127.4 | 86.7 | 420 → 3.2 |
| **Player total, STD only** | | | **38,885 → 335** | **228** | **~1,900 → 11.9** |
| **+ per-game + last-4 (decided)** | | | | | **~19** |
| `player_game_pbp` staging (L1 counts) | 330 | ~60 counts | | | 2.3 (L2 window) |
| Widened `ngs`/`pfr_advstats`/`ftn` | | | | | ~2.5 (L2 window) |
| `participation_player_season` | ~2,000/season | ~25 | | | 0.5/season; 2016–2024 backfill 4.5 once |
| Scheme, team-level (~60 signals, estimate) | 32 | | 1,920 → **16.5** in `signals` | | |
| Intel/news (volume unverified) | | | | | 5–20/season; ~1–2 with 30-day retention |

The shape is the lever (~28×), not the population. A ≥25%-snap filter would cut ~22% of
rows, ~2–3 MB/season in the wide design, and it only removes players whose values are
mostly noise anyway. Not used.

### Runway with the decided policies
Assumes the planned `injuries_backup_pre_changelog` drop (−33 MB, P6 §8 Q3), and P7
shipping ~2026 week 6 with a 2025 backfill (collapsed under L4 immediately).

| Scenario | End 2026 | Growth/season after | Crosses 500 MB |
|---|---|---|---|
| Status quo, no P7 | ~262 | 139 | ~Dec 2028 |
| P7 in `signals`, naive (rejected) | backfill alone 192 + 351 = 543 | ~490 | on the 2025 backfill run |
| **Decided: wide tables + L2 + L4 (no L3/L5/archive)** | **~320** | **~129** | **~Nov 2028** |

- The decided path crosses slightly *before* the status quo. L2 saves ~29 MB/season,
  but P7 adds a one-time ~58 MB in 2026 and ~19 MB/season permanently (scheme 16.5,
  collapsed player rows ~2, participation 0.5).
- Of the 129 MB/season, **injuries is 75**. Cutting injuries growth by ~40 MB/season
  buys about one more season (crossing ~Nov 2029). That's why open item 1 comes first.
- The archive, deferred, is what removes the ceiling: a ~290 MB plateau, at $0.
- Pro is only required for player detail in the `signals` shape, or for all history
  kept live without an archive.

### Retention policies (decided)
- **L2, staged player tables:** `player_week`, `snaps`, `ngs`, `ftn`, `pfr_advstats`,
  and `player_game_pbp` keep `season >= current_season - 1`.
  - Everything older is re-fetchable from nflverse, and backfills already refetch via
    `seasons_override`.
  - `team_week` is exempt (tiny; the backtest uses 2018+). So are `depth` (latest only)
    and `participation_player_season` (0.5 MB/season; it *is* the prior).
- **L4, player tables:** once a season is complete (its last game has a final score in
  `games`, read by the L0 job), delete every `player_usage_week`/`player_eff_week` row
  for that season except each player's latest-week row.
  - Team-level `signals` keep full weekly history (the backtest reads it).
  - Collapsed history is recomputable by refetching the season and re-running the
    analysts.
- Both run in the L0 retention job with a dry-run mode that reports rows and MB before
  deleting.

### Existing research findings (2026-09-22, still valid)
**Free and already staged, thin reads:** `player_week` EPA ÷ attempts/targets/carries;
`ngs.avg_yac_above_expectation`, `ngs.avg_separation`;
`pfr_advstats.rushing_yards_before_contact_avg`; `player_week` air yards.

**Free but not yet staged** (a migration change to an already-verified source):
- `ngs`: `avg_cushion`, `percent_share_of_intended_air_yards`, `avg_time_to_los`,
  `avg_intended_air_yards`, `avg_completed_air_yards`, and `avg_time_to_throw`.
- `pfr_advstats` `def`: `def_targets`, `def_completions_allowed`, `def_yards_allowed`,
  `def_completion_pct`, `def_adot`, `def_air_yards_completed`, `def_yards_after_catch`,
  `def_times_blitzed`, `def_times_hurried`, `def_times_hitqb`, `def_sacks`,
  `def_tackles_combined`, `def_missed_tackles`, `def_ints`, `def_receiving_td_allowed`,
  `def_yards_allowed_per_cmp`, `def_yards_allowed_per_tgt`. The `def` stat_type has no
  `position` column; join `snaps.position`.
- `ftn`: the 13 unstaged columns listed in the inventory.

**The load-bearing new engineering is the player-keyed pbp aggregation.**
- `player_week` has no success flag, and pbp's `run_location`/`run_gap` are never
  aggregated to a player grain.
- `player_game_pbp` (counts and sums, grouped by passer/rusher/receiver id, joined to
  FTN by `game_id`/`play_id`) supplies:
  - success rate
  - rush EPA/success by gap and direction
  - stuffed rate (`yards_gained <= 0`)
  - FTN-flag splits per player
- The team-defense-by-gap split is its team-level companion.

**Pressure rate / run-stop rate (unit-side):**
- Pressure rate: `def_pressures` over a pass-attempts-faced denominator.
- PFF-style per-defender run stops have **no free source**. PFR `def` is
  pass-defense-only. The honest substitute is team rush EPA/success by gap.

**Alignment (slot/perimeter) matchups are dropped from scope (decision, 2026-09-22).**
- No receiver-alignment or nearest-defender-role field exists in any free source.
- Where a directional split helps, use pbp's `pass_location`, labeled as a field-location
  split, never as slot/perimeter.

## Build steps (one go-ahead each)
1. **Docs** (done 2026-09-25): this file, `docs/architecture.md`, `docs/signals.md`
   ("Player tables"), `docs/sources.md`, `CLAUDE.md` layer-rule amendment.
2. **Verify + fixtures:** trimmed participation fixture via
   `scripts/make_nflverse_bulk_fixtures.py`; confirm the unstaged FTN/NGS/PFR columns
   against existing fixtures. Open item 1 (injuries churn) investigation. Open item 2
   license quotes.
3. **Migrations** (new files only): widen `ngs`/`pfr_advstats`/`ftn`;
   `player_game_pbp`; `participation_player_season`; `player_usage_week`;
   `player_eff_week`; `news_items`/`news_tags`. All RLS default-deny.
4. **Collector:** extend `nflverse_bulk.py` with the new datasets.
   - `player_game_pbp` depends on two tags (`pbp`, `ftn_charting`).
   - `participation_player_season` fetches `season - 1` only (nflreadpy raises for the
     current season) under tag `pbp_participation`.
5. **Retention job** (`pipeline/orchestration/retention.py`), dry run first.
6. **Analysts:** `usage.py`, `player_efficiency.py` (reuse `efficiency.py`'s blend/
   stability helpers and `filter_changed`), `scheme.py`.
7. **Backfill 2025 + storage check:** the user runs it. Measure
   `pg_total_relation_size / count(*)` per new table against the estimates above. Stop
   and re-plan if any table exceeds 1.5× its estimate.
8. **Intel collector.**
9. **Web player view** (its own migration, P6 §2 update, CLAUDE.md L3 amendment).
   Blocked on open item 2.

## Files to create
```
pipeline/analysts/usage.py
pipeline/analysts/player_efficiency.py
pipeline/analysts/scheme.py
pipeline/orchestration/retention.py
pipeline/collectors/intel.py
db/migrations/00NN_ngs_pfr_ftn_columns.sql
db/migrations/00NN_player_game_pbp.sql
db/migrations/00NN_participation_player_season.sql
db/migrations/00NN_player_usage_week.sql
db/migrations/00NN_player_eff_week.sql
db/migrations/00NN_news.sql
tests/fixtures/nflreadpy_participation_sample.parquet
tests/fixtures/espn_news_sample.json
tests/test_usage.py
tests/test_player_efficiency.py
tests/test_scheme.py
tests/test_retention.py
tests/test_intel.py
```
(`pipeline/collectors/nflverse_bulk.py` and its tests are extended, not new.)

## Done when
- Usage shares sum sanely per team per week (target shares ≈1.0 across pass catchers,
  allowing for nulls).
- Player efficiency values exist per player-week and are spot-checked against pbp by hand
  for at least one player, via an `inspect_*` script that imports the analyst's own
  functions.
- Scheme signals (including run-defense-by-gap and coverage-proxy splits) written and
  spot-checked. Every participation-derived value carries `_hist` and its season span.
- Retention dry run reviewed, then run. Measured bytes/row within 1.5× of the estimates.
- News items deduped and linked to teams/players via the ID crosswalk. No LLM calls in
  the tagging path.
- Nothing in `docs/signals.md` describes PFR's nearest-defender stats, pbp defender
  credits, or `pass_location` splits as true coverage/alignment matchups.
