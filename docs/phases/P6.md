# P6 — Data sheet web app (v1: read-only week + game views)

**Status:** step 3 built and verified (2026-09-24), awaiting approval. Steps 0–2 are
done. §8's open questions are decided.
- **Step 0 is done:** commit `9386e3e`, pushed.
  - Migration `0026` applied, then Exposed schemas set to `web` only.
  - The full `scripts/verify_anon_access.py` passed on 2026-09-24: every catalog check,
    all API reads, every write rejected, `public` not exposed (PGRST106), RPC
    unreachable.
  - Before the migration, the same script failed every check that 0026 changes.
- **Step 1 is done:** versions pinned (§1), the odds terms quoted in `docs/sources.md`.
  The `web`-schema question was decided as (a), leave as is.
- **Step 2 is done:** the `/web` scaffold (step 2 results below).

## 0. What v1 is, and what moved out

v1 is a read-only app with two things in it:
- a **week view** that lists games;
- a **game view** that shows the game's matchup card and the signals behind it.

Nothing else.

**Replaced from the pre-P5 stub. Confirmed 2026-09-24; `KICKOFF.md`'s P6 rows were
updated to match:**

| Old P6 item | v1 decision | Why |
|---|---|---|
| Player view, filter players | Moved to P7 | Player-level signals that pair against units don't exist yet. Today's player rows (availability) have `team` null and no names readable by L3 (§8 Q2). |
| Cross-reference any signal | Deferred (v2) | Needs a query surface over all 163k rows. v1 shows the signals behind one game. |
| Staleness badges from the auditor | Replaced | Freshness comes from `as_of` and the lead-hour signals already on cards/signals. Reading `auditor_alerts`/`source_freshness` would widen the anon surface for little gain. |
| "Reads Supabase only via views" | Kept, sharpened | The boundary is grants + RLS. The views are convenience (§2). |

**Licensing obligations carried over from the stub:**
- **Open-Meteo (weather), CC BY 4.0.** Put the credit ("Weather data by
  Open-Meteo.com", linked to https://open-meteo.com/) on the weather block itself, not
  only a site-wide footer. A card can be viewed or screenshotted without the footer.
- **Open-Meteo's free tier is non-commercial.** No ads, subscriptions, or paywall.
  Adding any of those means moving to a paid plan first.
- **OpenStreetMap (stadium coords / `field_bearing`), ODbL.** Credit "© OpenStreetMap
  contributors" wherever stadium-derived data is shown, or on `/sources` linked from the
  weather block.
- **Wind wording.** Values are exterior 10 m estimates. Label them "Outside wind (10 m
  est.)", never "wind at the field".

### Signals that exist today (live, 2026-09-24)

v1 is designed against exactly these. `league_pct` is **null on every row in every
sector**, so v1 shows no ranks or percentiles (§8 Q5).

| Sector | Signals | Scope | Coverage | Rows |
|---|---|---|---|---|
| efficiency | **40**: 20 bases × `_off`/`_def` (`epa_per_play`, `_pass`, `_rush`, `_down1..4`; `success_rate` same 7 splits; `explosive_rate`, `_pass`, `_rush`; `points_per_drive`; `three_and_out_rate`; `red_zone_td_rate`) | team-week (`game_id`, `player_id` null) | 2019 wk1 → 2026 wk3, 126 weeks, 1,280 rows/week | 4,032 each, 161,280 total. `sample_n` and `stability` populated. |
| availability | **7**. Team (2): `ol_cluster_count` 32, `secondary_cluster_count` 56. Player, `team` **null** (5): `availability_category` 837, `practice_trend_risk` 806, `snap_share_redistribution_gain` 636, `replacement_depth_rank_delta` 292, `snap_share_at_risk` 203. | team-week / player-week | 2026 wk2–3 only | 2,862 |
| environment | **20 observed** + 2 registered but not yet seen (`wind_along_field_mph`, `wind_crosswind_mph`, mode 3 only). Game (15): `weather_status`, `venue_roof_code`, `surface_code` (16 rows each); `temperature_f`, `apparent_temperature_f`, `precip_total_in`, `snowfall_total_in`, `precip_prob_max_pct`, `wind_speed_mph`, `wind_gust_max_mph`, `wind_direction_mode`, `weather_lead_hours`, `weather_model_regime_break`, `weather_forecast_domain`, `venue_elevation_m` (1 row each: only TNF has a capture so far). Team (5): `rest_days`, `rest_diff`, `travel_miles`, `tz_shift_hours`, `tz_offset_diff_raw_hours` (32 each). | game / game-team | 2026 wk3 only | 220 |
| market | **25**. Game (23): `market_status`, `market_open_basis`, `market_own_week_captures`, `market_open_lead_hours`, `market_current_lead_hours`, `spread_home_open`/`_current`/`_move`, `spread_move_per_day`, `spread_book_range`, `spread_key_straddle`, `spread_key_crossings`, `spread_book_set_changed`, `spread_book_count_open`/`_current`, and the `total_*` counterparts (`total_open`, `total_current`, `total_move`, `total_move_per_day`, `total_book_range`, `total_book_set_changed`, `total_book_count_open`/`_current`). Team (2): `implied_team_total`, `win_prob_novig` (32 each). | game / game-team | 2026 wk3 only | 432 |
| usage, scheme, matchup | **0**. Allowed by the CHECK constraint, not built (P7). | | | 0 |

**`matchup_cards`:** 16 rows, all 2026 wk3, all `projection_status = 1`, 1 locked
(`2026_03_ATL_GB`). A card averages 18 KB of JSON (`card_version` 1). Its blocks:
- `identity`
- `projection`:
  - `spread_home`, `total`, `pts_*`
  - `decomposition`: per side, α + HFA + **2 terms** (`epa_per_play_off` of the offense,
    `epa_per_play_def` of the opponent)
- `uncertainty`:
  - 4 stabilities, `stability_min`, `stability_bucket`
  - `edge_validated` {spread, total}, `edge_note`
  - `outcome_noise`
- `market`:
  - `status`, the 23 game signals, the team signals
  - `in_model: false`
- `edge`:
  - `vs_current`: values, flags, `flag_notes`, `summary`
  - `at_lock`
- `lock`
- `context`: `in_model: false`
  - `efficiency_pairings`: **19 per side**, the 20 bases minus the model's
    `epa_per_play`, built by `pair_unit_signals`
  - `environment` {game, teams}
  - `availability`: the 2 team counts

Games before 2026 wk3 have no card.

## 1. Stack, with free-tier math

| Piece | Choice | Why |
|---|---|---|
| Framework | Next.js App Router + TypeScript strict. Versions pinned at step 1 (below). | Per CLAUDE.md. |
| Rendering | **Server Components only, ISR** (`revalidate = 600`). Zero client components in v1. `<details>` for collapsibles. | Supabase sees page regenerations, not visits. Pages keep working from cache if Supabase hiccups (ISR keeps the last good render on a failed revalidate). |
| Data access | Plain `fetch` to PostgREST (`/rest/v1/<view>`, `Accept-Profile: web`) from **one module**, `web/lib/db.ts`. No `supabase-js`. | The read contract (§2) is one file. Next's fetch cache integrates with ISR. One fewer dependency. |
| Styling | Plain CSS + custom properties (`web/app/tokens.css`). No Tailwind, no component kit. | The visual spec (§4) lives in one tokens file. No framework defaults to fight. |
| Fonts | IBM Plex Sans + Plex Mono via `next/font/google` | Self-hosted at build, so no runtime third-party call. |
| Other deps | `zod` (card parse), `marked` (render `backtest_report.md` server-side), `vitest` (dev) | Each is server/build-only, so nothing reaches the client bundle. |
| Host | Vercel Hobby, root dir `web/`, default `*.vercel.app` domain | A custom domain costs money (§8 Q8). |

**Pinned versions (step 1, 2026-09-24).** Exact pins, no ranges. Latest per `npm view`
unless noted.

| Package | Pin | Note |
|---|---|---|
| `next` | 16.3.6 | Docs checked via context7 against v16.2.9, the newest version it indexes. The APIs used below have been unchanged since 13.x. |
| `react`, `react-dom` | 19.3.0 | Within next's peer range `^18.2.0 \|\| ^19.0.0`. |
| `typescript` | **6.0.3, not 7.0.2** | `latest` is 7.0.2, the Go-native compiler. Next 16's documented minimum is 5.1. Next.js's TS 7 bin handling was seen only in canary source, so 16.3.6's support is unverified. Revisit once a Next release notes TS 7. |
| `@types/react` | 19.3.0 | |
| `@types/node` | 24.13.6 | Latest 24.x, matching the runtime rather than latest (26.6.2). |
| `zod` | 4.6.5 | |
| `marked` | 18.0.14 | |
| `vitest` | 5.0.1 | dev |
| Node | `engines.node: "24.x"` | Vercel's supported majors are 24.x (default), 22.x, 20.x (20 deprecated 2026-10-01). Local is 24.15.0. |

**Rendering facts verified for Next 16 (context7, `/vercel/next.js/v16.2.9`):**
- Leave `cacheComponents` off, which is the default. With it on, `export const
  revalidate` is a build error, and `generateStaticParams` can't return `[]`.
- `export const revalidate = 600` is route-segment time-based ISR: serve stale, then
  regenerate in the background.
- **Dynamic routes (`/[season]/[week]`, `/game/[gameId]`) must export
  `generateStaticParams` returning `[]`.** Otherwise paths aren't ISR-cached at runtime.
  Quoting the docs: "To enable revalidation (ISR) for paths at runtime, you must return
  an empty array from `generateStaticParams` or use `export const dynamic =
  'force-static'`."
- `dynamicParams` stays at its default `true`, so every path renders on first request.

**Key type:** `.env`'s `SUPABASE_ANON_KEY` is the **legacy anon JWT**, not a
`sb_publishable_…` key.
- Whether the project also offers a publishable key is only visible in the dashboard
  (Settings → API Keys). None of the available MCP tools reads keys.
- Rotating a legacy key means rotating the JWT secret, which also rotates the
  service-role key. The pipeline uses neither, so rotation is still harmless here.

**Hosting:** the repo `BrodyBoShow/nfl-data-sheet` is owned by a GitHub **User**
account, not an org (checked with `gh api users/BrodyBoShow`), so it can connect to a
Hobby project.

**Free-tier limits (fetched 2026-09-24: vercel.com/docs/limits,
/docs/limits/fair-use-guidelines, supabase.com/pricing) vs. an estimate at 100
visits/week (~430/month, ~4 pages/visit):**

| Resource | Free limit | Estimate | Headroom |
|---|---|---|---|
| Vercel Fast Data Transfer | 100 GB/mo | ~150 KB gz first load × ~1,700 pages ≈ **0.3 GB** | ~99.7% |
| Vercel Function invocations | 1,000,000/mo | ISR regenerations only: ≤ 1 per page per 10 min when visited, **≤ ~2k** | >99% |
| Vercel Active CPU | 4 h/mo | ~2k renders × ≤0.1 s ≈ **0.06 h** | ~98% |
| Vercel Fast Origin Transfer | 10 GB/mo | **< 0.1 GB** | >99% |
| Vercel Image Optimization | 5k transforms/mo | **0** (no images, no logos) | n/a |
| Vercel builds | 100 deploys/day, 45 min/build | a few per week. The ignored-build step skips pipeline-only pushes (step 9). | n/a |
| Supabase egress | 5 GB/mo (+5 GB cached) | detail regeneration ≈ 18 KB card + ~35 KB raw rows; week ≈ 20 KB. ≤ 2k regenerations ≈ **0.1 GB web-side** | ~98% of the web share, **but the pipeline shares this quota** (§8 Q9) |
| Supabase DB size | 500 MB | 191 MB now. Views add 0. Signals grow ~10 MB/season. | ~60% |
| Supabase API requests | unlimited | | n/a |
| Supabase pausing | "Free projects are paused after 1 week of inactivity" | Pipeline ticks keep it active in season. | **Offseason risk** (below) |

**Cost is capped at $0 even under abuse, because neither free plan has overage billing.**
- Vercel Hobby pauses the project at its limits.
- Supabase free with no card restricts the project; it doesn't bill.

The real risk is **availability**:
1. **Shared Supabase egress.** The web app and the pipeline share one Supabase project.
   If the web app burned the egress quota, the *pipeline* would be restricted too. ISR
   is the main defense: Supabase traffic is regenerations, not visits.
2. **Offseason pause.** GitHub disables scheduled workflows after 60 days of repo
   inactivity. Supabase then pauses after 7 idle days. Cached ISR pages keep serving,
   but uncached pages error. Not a v1 problem; noted for the offseason.

Nothing at 100 visits/week comes near a limit. The first limit a large traffic spike
would reach is Vercel's 100 GB transfer, at roughly 300× the estimate.

Hobby is **non-commercial only** (Vercel's fair-use page), which matches Open-Meteo's
free-tier terms. No ads, donations-only at most.

## 2. Read contract and anon-key scope

### 2a. Access model: migration `0026_web_read_access.sql` (step 0)

- **Grants and RLS are the boundary. Views are convenience.** Row scope lives in RLS
  policies and column scope in column grants. So even if `public` were re-exposed
  through the API, anon would still see only what's listed here.
- **Close everything first:**
  - `REVOKE ALL` on every table, sequence, and function in `public` from `anon` and
    `authenticated`.
    - Before 0026, anon held full grants on **every** public table. RLS default-deny
      (0007) was the only thing blocking it.
  - `ALTER DEFAULT PRIVILEGES FOR ROLE postgres IN SCHEMA public REVOKE ALL ... FROM
    anon, authenticated`.
    - Live `pg_default_acl` showed `postgres` auto-granting anon full rights on every
      new public table, sequence, and function.
- **Found while planning, fixed in 0026:** `injuries_backup_pre_changelog` (25,653 rows,
  including raw ESPN/Sleeper payloads) had **RLS disabled** and full anon grants.
  - It was readable and writable by anyone holding the anon key. The key hadn't shipped
    anywhere, so the exposure was latent.
  - Drop it after Sunday 2026-09-27's slate runs clean through the changelog path
    (§8 Q3).
- **Then open exactly three tables, read-only:**

| Table | Column grant to `anon` | RLS policy (`FOR SELECT TO anon`) |
|---|---|---|
| `games` | `game_id, season, week, home_team, away_team, gameday, gametime, location`. Exactly the L3 identity set, so scores and lines are unreadable at the grant level. | `USING (true)` |
| `matchup_cards` | every column except `content_hash`, `updated_at` | `USING (true)` |
| `signals` | every column except `id` | `USING (player_id IS NULL)`, so v1 exposes no player rows (§8 Q2) |

- **Schema `web`:** `security_invoker = true` views over those grants (they pass
  Supabase's security linter, which flags definer views). `GRANT USAGE ON SCHEMA web`
  and `SELECT` on the views to anon.
- **Dashboard step (user, after 0026 is applied):** Data API → Exposed schemas = **`web`
  only**.
- **No write path exists for anon:** no INSERT/UPDATE/DELETE grant and no write policy
  on any object.
- **Two things 0026 doesn't touch:**
  - `supabase_admin` has the same permissive default ACL in `public`. `postgres` can't
    alter it. It only applies to objects `supabase_admin` itself creates, and every
    table here is owned by `postgres`.
  - `pg_graphql` isn't installed, so there's no GraphQL surface.

### 2b. Views

| View | Source | Columns |
|---|---|---|
| `web.games` | `games` | the 8 identity columns |
| `web.weeks` | `games` ⟕ `matchup_cards` | `season, week, first_gameday, last_gameday, n_games, n_cards` |
| `web.week_cards` | `matchup_cards` | `game_id, season, week, kickoff, projection_status, locked, as_of, projected_spread, projected_total`, plus jsonb-extracted scalars (extraction and casts only, no arithmetic): `projection_status_label, stability_bucket, stability_min, market_status, market_spread_latest, market_total_latest, lock_market_spread, lock_market_total, locked_at, locks_from, weather_status, venue_roof_code, temperature_f, wind_speed_mph` |
| `web.cards` | `matchup_cards` | `game_id, season, week, kickoff, projection_status, locked, card, as_of, inputs_version` |
| `web.signals` | `signals` | `season, week, game_id, team, player_id, sector, signal, value, league_pct, sample_n, stability, as_of, inputs_version` |

### 2c. Every query the app makes (all in `web/lib/db.ts`, nowhere else)

| # | Used by | Query |
|---|---|---|
| Q1 | nav, `/` redirect | `web.weeks` all rows (≈150), ordered |
| Q2 | week view | `web.games?season=eq.S&week=eq.W&order=gameday,gametime,game_id` |
| Q3 | week view | `web.week_cards?season=eq.S&week=eq.W` |
| Q4 | game view | `web.games?game_id=eq.G` |
| Q5 | game view | `web.cards?game_id=eq.G` (0 or 1 row) |
| Q6 | game view, raw-rows appendix / no-card games | `web.signals?season=eq.S&week=eq.W&or=(and(game_id.is.null,team.in.(H,A)),game_id.eq.G)`. About 80 efficiency + ≤4 availability + ~50 env/market rows. |

**Time handling.** `games.gameday`/`gametime` are already ET: `kickoff_utc`'s docstring
says so, verified 2026-09-18.
- The UI prints them as ET directly, with no conversion.
- Card kickoffs (UTC) are formatted to ET for display.
- **No relative times** ("in 3h") anywhere, because ISR pages are cached. Every page
  prints "page built HH:MM ET".

### 2d. What an attacker holding the key can pull

**Treat the key as public**, even though v1 keeps it **server-only** (env var without
`NEXT_PUBLIC_`, so it never reaches the browser bundle; step 9 greps the build output).
With the key and the project URL, an attacker can:

- **Read** all 2,499 schedule rows, 8 identity columns each.
- **Read** every matchup card, including frozen ones (~270/season). That's exactly what
  the site renders.
- **Read** every team- and game-scope signal row since 2019 (~163k rows, ~40 MB as
  JSON). Also exactly what the site renders, in bulk. PostgREST caps each request at
  1,000 rows, so a full dump is ~165 requests.
- **Nothing else:**
  - no player rows;
  - no scores or lines;
  - no staged tables, no raw per-book odds, no injury text;
  - no `agent_runs` errors/meta, no `projection_log`/grades, no `players`/crosswalk;
  - no writes;
  - no RPC.

**Worst realistic abuse:** repeated bulk dumps to burn the 5 GB egress. At ~40 MB per
dump that takes ~125 full dumps in a month, and it restricts the shared project (§1).
**Response:** rotate the key. Prefer the new `sb_publishable_…` key if the project has
one, since it rotates without touching anything else; the pipeline uses
`SUPABASE_DB_URL`, not an API key.

`scripts/verify_anon_access.py` (step 0) asserts all of the above live, so this section is
tested rather than just claimed.

## 3. Routes

| Route | Content | Queries |
|---|---|---|
| `/` | Redirect to the first week whose `last_gameday ≥ today (ET)` | Q1 |
| `/[season]/[week]` | Week view, e.g. `/2026/3`. Pre-2026 weeks are linked in the nav, marked "no card" (§8 Q6). | Q1–Q3 |
| `/game/[gameId]` | Game view, e.g. `/game/2026_03_ATL_GB` | Q4–Q6 |
| `/method` | How the model works, what "input stability" means, and the full backtest report with a summary on top. `#edge` anchor. | none (build-time content) |
| `/sources` | Data sources and licenses: nflverse CC-BY 4.0, FTN CC-BY-SA 4.0, Open-Meteo CC BY 4.0, OSM ODbL, The Odds API per its terms, ESPN/Sleeper derived counts only | none |

**Games without a card:**
- 2019–2025: game views show the `efficiency` rows entering that week (Q6), with no
  projection and the note "no card: in-sample season".
- 2026 wk1–2: game views say "no card: synthesizer started 2026 wk 3".
- Week views for those weeks list the schedule only.

**Build-time content:** a `prebuild` script copies `docs/backtest_report.md` and
`pipeline/synthesis/model_coefficients.json` into `web/content/` (gitignored). The build
fails if either is missing, or if the report's "All" rows (margin/total MAE, pooled edge
r/CI) can't be parsed. These are repo artifacts of L3's own synthesis step, not a data
source, and the app never reads them at runtime.

**P7's structural place:**
- The context table renders rows shaped like `pair_unit_signals` output
  (`{base, subject:{team, player_id}, opponent:{…}}`).
- Row labels come from one resolver, `subjectLabel(row)`: the team code now, a player
  name later.
- Signal display names come from `web/lib/signal-labels.ts`, and an unknown signal name
  falls back to its raw name. So new P7 signals render without a code change.
- A P7 player row slots under its unit (e.g. "ATL pass offense → WR rows") in the same
  table. No route or IA change. **Nothing else in v1 is built for P7.**

## 4. Visual spec

Decided once here, implemented once in `web/app/tokens.css`. The target is a statistical
almanac or terminal: data starts within the first ~100px of every page.

**Type.** IBM Plex Sans for UI and prose; IBM Plex Mono for every number, team code, and
signal name. Weights 400/500/600 only.

| Token | px / line-height | Use |
|---|---|---|
| `--t-cap` | 11/14, 500, uppercase, +0.06em | column heads, section labels, tags, footnotes |
| `--t-small` | 12/16 | secondary cells, raw signal names, `as_of` stamps |
| `--t-base` | 13/18 | table cells, default UI |
| `--t-prose` | 15/22 | `/method`, `/sources` body only |
| `--t-head` | 16/20, 600 | section heads |
| `--t-title` | 20/24, 600 | one per page (e.g. `ATL @ GB`) |

No other sizes.

**Numerals and values:**
- Tabular, lining figures, right-aligned, with a fixed number of decimals per signal
  type (EPA 3, rates as % with 1, points 1, lines 1).
- Minus is U+2212.
- Spreads are team-labeled (`GB −4.2`, `PK`), never the raw home-negative number.
- Null is `—` in `--ink-3`, with a `title` giving the reason where known (e.g. "no row:
  fewer than 2 books"). Null is never shown as 0.

**Palette.** No green/red, no team colors, no indigo/purple, no gradients.

| Token | Light | Dark | Use |
|---|---|---|---|
| `--bg` | `#F7F6F2` | `#101112` | page |
| `--bg-alt` | `#EFEDE6` | `#17191B` | header rows, day-group rows |
| `--ink` | `#16181A` | `#E4E2DC` | data |
| `--ink-2` | `#5B5F63` | `#9A9EA2` | labels, secondary |
| `--ink-3` | `#8C9094` | `#6A6E72` | nulls, raw names, stamps |
| `--rule` | `#D6D3CA` | `#2A2D30` | 1px hairlines |
| `--link` | `#1D4F73` | `#8DB7D6` | links, always underlined |
| `--warn` | `#8A5300` | `#D9A13B` | **pipeline state only:** stale, missing, not validated, not projected |

Dark mode follows `prefers-color-scheme`.

**Density and layout:**
- Max width 1120px, left-aligned, 16px gutters.
- No hero, no marketing copy, no cards-as-boxes, no shadows. Border radius 0 (2px max on
  tags).
- Sections are separated by a 1px rule plus a `--t-cap` label.
- The game view uses 2 columns at ≥960px (left: lines and the model's arithmetic; right:
  context), 1 column below that.
- Wide tables scroll inside their own container with a sticky first column. The page
  itself never scrolls sideways.
- No icons, emoji, logos, or images.

**Table treatment:**
- Row height 24px, cell padding 2px 8px.
- Head row in `--t-cap` `--ink-2` with a bottom rule. No zebra striping.
- Day groups (THU / SUN / MON) are full-width `--bg-alt` rows.
- Units go in headers, not cells.
- In signal tables, value · n · stab are always adjacent columns. A signal cell shows its
  display name, with the raw `signal` name beneath in `--t-small` mono `--ink-3`.
- **Every signal table reserves a `Rank` column** right after the value, rendering `—`
  (title: "league rank not computed yet") until Efficiency fills `league_pct` (P2 pending
  amendment). Filling it later is a data change, not a layout change (§8 Q5).

**Week view columns:**
Kickoff (ET) · Matchup (`ATL @ GB`, neutral marked `vs`) · Market spread (latest) · Model
spread · Market total · Model total · Stab (`LOW`/`MID`/`HIGH` as text) · Status
(`LOCKED 15:28` / `PROVISIONAL · locks 18:15` / the status label when not projected) ·
Venue (roof code, temp/wind only when `weather_status = 1`).

**No edge column** (§5, §8 Q1). Default sort is kickoff, and no column is sortable in v1.

## 5. The honesty requirement, concretely

The approach: **attach the evidence to the number, instead of stacking disclaimers above
it.** A reader who knows football reads a correlation of −0.03 faster than a paragraph.
There are three devices, each used once, plus vocabulary rules.

1. **A status line in the header of every page.** One line, `--t-small` mono, parsed
   from the backtest report at build time:
   `Backtest 2020–25, 1,615 games out of sample: margin MAE model 10.32 · closing line 9.76 ·
   model − market vs result r −0.03 [−0.08, 0.02] · edge not validated → method`
   - It isn't dismissable and isn't a banner. It sits in the header rule, like a
     terminal status bar.
   - The backtest is one click from every page (`/method`), and the result is readable
     without that click.
2. **An inline tag on every edge value.** The edge only appears on the game view, as a
   row labeled `Model − market`. The tag is part of the value, not a footnote:
   `0.3 toward ATL  [NOT VALIDATED]`
   - The tag has a `--warn` hairline border and links to `/method#edge`.
   - Its `title` holds the card's bucket evidence, e.g. "Backtest 2020–25, low input
     stability (n 535): r −0.024 [−0.108, 0.056]."
   - `validated` comes from `card.uncertainty.edge_validated`; the r/CI/n come from
     `model_coefficients.json` → `calibration.buckets[bucket].edge_validation`. They're
     shown only when the card's `projection.model_version` equals the file's; otherwise
     the tag shows the flag alone.
   - If a future model is ever validated, the tag reads `[VALIDATED]` with the same
     treatment: no color, no emphasis.
3. **The stability bucket sits next to every projection.** That's a `Stab` column in the
   week view, and a row directly under the model line in the game view:
   `Input stability  LOW (0.49)`
   - Its `title`: "Share of each rating that comes from this season's plays and a
     trusted prior, rather than the league average. Not confidence in the result."
   - Right below it, once per game: `Typical miss, any projection (model or market)  ≈13
     pts`, from `uncertainty.outcome_noise`. This puts the model and the market on equal
     footing instead of implying a precise model.

**What the app never does:**
- **No pick vocabulary** in UI strings: pick, play (as a bet), bet, best, lean, value,
  fade, hammer, sharp, lock (as a bet), confidence, recommend.
  - "Lock" appears only as the pipeline term `LOCKED`, with its time.
  - The card's `edge.vs_current.summary` ("model favors ATL by…") is **not rendered**;
    the app shows neutral `toward ATL` instead.
- **No emphasis on edges:** no ranking, sorting, filtering, or highlighting by edge. No
  "biggest edges" list. Edge digits use the same `--ink`, weight, and size as every
  other number: no bold, arrows, or check marks.
- **No edge in the week view.** Market and model lines sit side by side, so the gap is
  visible, but there's no scannable edge column. A scannable edge column trains exactly
  the behavior the backtest says is unjustified (§8 Q1).
- **No record, win rate, or ROI anywhere in v1.** Grades aren't exposed (§8 Q7).
- **No projection shown when `projection_status ≠ 1`**, only the status label in
  `--warn`.
- **Before the lock**, the projection is marked `PROVISIONAL · locks from 07:00 ET`, so
  a moving number isn't read as the claim.
- **After the lock**, the lines block shows three columns: `Market at lock` · `Latest
  pre-kickoff market` · `Model (locked)`.
  - Nothing is computed; the values come from `edge.at_lock` and `edge.vs_current`.
  - This keeps the week-3 stale-line problem visible (ATL@GB: −6.5 at lock vs −4.5
    latest) instead of hiding it.
- **"In the model" and "context" are always separate sections:**
  - The model section is the arithmetic from `projection.decomposition`:
    `α + HFA + β_off × centered + β_def × centered = points`, per side, every value read
    from the card.
  - Everything else sits under `Context: not used by the model` (the card's own
    `in_model: false`).
- **Precision is capped at 1 decimal** for projections and edges.

**Enforced by test (step 8):**
- A vitest suite renders the fixture pages and fails on the banned vocabulary.
- It fails on any edge value without a sibling tag.
- It fails on any projection without a stability bucket in the same row/section.

## 6. Game view layout (single card, top to bottom)

1. **Title row:** `ATL @ GB`, then `2026 · WK 3 · THU 09-24 20:15 ET · home` (or
   `neutral`), then the status (`LOCKED 15:28 ET, 4.8 h pre-kickoff` / `PROVISIONAL` /
   the status label), then `card as of …` (`matchup_cards.as_of`, which is when the
   content last changed).
2. **Lines** (left): spread and total rows × market columns and the model column, plus
   the `Model − market` row with its tag, the stability row, and the typical-miss row.
3. **Model arithmetic** (left): the two-side decomposition table. For each term: value
   · week mean · centered · β · contribution · n · stab. `model p5-v1`.
4. **Context: not used by the model** (right):
   - Efficiency pairings, two tables (`GB offense vs ATL defense`, reverse), 19 rows
     each: display name / raw name · off value · rank · n · stab · def value · rank · n
     · stab.
   - Market detail: open/latest/move/per-day, book range, book counts, set changed,
     crossings, straddle with its card note, open basis, capture lead hours.
     `implied_team_total` and `win_prob_novig` go under a "market-implied" label.
   - Environment: weather status, roof, surface, and the weather values labeled
     `Outside wind (10 m est.)` with the **"Weather data by Open-Meteo.com" credit on
     the block itself**. Rest, travel, and tz per team, with an OSM credit link to
     `/sources`.
   - Availability: OL and secondary flagged counts per team.
5. **Raw signal rows** (`<details>`, collapsed): the Q6 rows as a flat table with
   `as_of`. Headed: "Current table values. The card above is frozen at its last change
   and may differ."

## 7. Build steps

Each step is gated on the one before it, and each ends with a commit after user approval.
Step 0 is committed on its own.

**Standing rule (user, 2026-09-24): every check that verifies something must first be
shown capable of failing.**
- Before a done-when check is trusted, run it against a deliberately broken state and
  confirm it fails.
- Each step report says what was broken and that the check failed correctly.
- Why: step 2's first layout check returned a false PASS with no CSS loaded. It was
  measuring a stale server whose stylesheets 404'd, and only checked for horizontal
  overflow, which an unstyled page doesn't have either.
- `verify_anon_access.py` was run against the pre-migration database first, and that's
  the model to follow.

| # | Step | Done when |
|---|---|---|
| 0 | **Close the anon surface.** It's independent of the app, because the anon grants are a live hole, not a P6 prerequisite. `db/migrations/0026_web_read_access.sql` (§2a–b), `scripts/verify_anon_access.py`, `SUPABASE_ANON_KEY` in config. **Order matters:** apply 0026 *before* changing Exposed schemas, because `web` must exist first. | Script passes live: <br>• the anon catalog privileges are exactly §2a; <br>• RLS is on for every public table; <br>• each `web` view returns rows; <br>• `web.games` has exactly 8 columns; <br>• `web.signals` has 0 player rows; <br>• writes to every view are rejected with 42501; <br>• `public` is not exposed. <br>Security advisor shows no new ERROR lints. |
| 1 | **Verify before scaffolding.** Pin the Next.js version and ISR API via context7. Confirm the project has a publishable key. Read The Odds API's terms on public display of derived data and record them in `docs/sources.md` with URL + date. Confirm the repo is on a personal account (Hobby can't connect org repos). | `docs/sources.md` has the odds display note. §1 names the pinned versions. **If the odds terms forbid public display of derived data, stop and tell the user before re-planning.** That changes what the app is. The market block must not quietly disappear. |
| 2 | **Scaffold `/web`** with `tokens.css`, fonts, and a layout with the header status line (placeholder numbers) and a footer (`/method`, `/sources`, "not affiliated with the NFL"). | `npm run build` passes. The layout renders at 375px and 1280px with no horizontal page scroll. |
| 3 | **Read layer.** `web/lib/db.ts` (Q1–Q6, typed); `web/lib/card.ts` (zod schema for `card_version` 1); fixture `web/tests/fixtures/card_2026_03_ATL_GB.json` (a trimmed real card). An unknown `card_version` renders a "card format not supported" block, not a crash. | vitest: the fixture parses. A test asserts `db.ts` is the only file that reads `SUPABASE_URL` or calls `fetch`. |
| 4 | **Build-time content.** The `prebuild` copy plus parse of the report's "All" rows into the status line. | Deleting or renaming either source file fails `npm run build`. The status line shows 10.32 / 9.76 / −0.03 [−0.08, 0.02]. |
| 5 | **Week view** + `/` redirect + week nav, including pre-2026 weeks. | `/2026/3` renders 16 rows. 3 spot-checked games match `matchup_cards` via `scripts/inspect_synthesizer.py` or a Q3 read. `/2026/2` and `/2020/5` render the schedule with "no card". Every projected row shows a bucket. There's no edge column. |
| 6 | **Game view** (§6), including the status ≠ 1 and no-card states. | ATL@GB shows at-lock −6.5 / 44.5 beside latest −4.5 / 43.0. A test asserts the rendered decomposition terms sum to the card's own `points` (a display check on the card's numbers). Every null renders `—`. The rank column is present and renders `—`. The weather block carries the Open-Meteo credit and the 10 m label. |
| 7 | **`/method` + `/sources`.** | Every page links to `/method`. `#edge` resolves. `/sources` covers every source behind a displayed signal, with its license. |
| 8 | **Honesty test suite** (§5 enforcement). | vitest passes. Adding "best bet" to any component fails it. |
| 9 | **Deploy.** First record the pipeline's current monthly Supabase egress (Usage page) in §1. Then: Vercel Hobby, root `web/`, env `SUPABASE_URL` + `SUPABASE_ANON_KEY` (server-only). Build command `npm test && next build`. Ignored-build step: `git diff --quiet HEAD^ HEAD -- web/ docs/backtest_report.md pipeline/synthesis/model_coefficients.json`. | The egress baseline is in §1. The production URL serves `/`, `/2026/3`, and `/game/2026_03_ATL_GB`. `grep -r <key prefix> web/.next/static` finds nothing. Supabase egress is recorded here after 1 week. |
| 10 | **Docs.** This file's status and actual routes. `docs/architecture.md`'s web-app row and L3 read list. CLAUDE.md's L3 rule ("`/web` reads `signals`, `matchup_cards`, and `games` identity, only through the `web` schema"). | The docs match the code. |

### Step 3 results (2026-09-24)

- **`web/lib/db.ts`: the whole read contract (Q1–Q6).**
  - The only module that reads `SUPABASE_*` / `process.env` or calls `fetch`.
  - PostgREST against `web` as anon: `apikey` always, `Authorization: Bearer` only for a
    legacy JWT, never for a publishable key.
  - Every row is validated by zod at the boundary.
  - A result that reaches the 1,000-row cap is an error, never a silently truncated
    list.
  - `season`, `week`, `game_id` and team codes are validated before they reach a
    PostgREST filter string, so a route param can't inject filter syntax.
  - All queries are `async`: a bad param is a rejected promise, the same as a failed
    request.
- **`web/lib/card.ts`: zod schema for `card_version` 1**, mirroring `build_cards()`.
  - `parseCard` never throws. It returns `ok`, `unsupported_version` (any other
    version), or `invalid` with field paths.
  - Unknown keys are stripped. Changed or removed fields fail.
- **Fixtures** (`web/scripts/make-fixtures.mjs`, captured as anon through the same
  views, trimmed): two cards (ATL@GB locked, ARI@SF provisional; efficiency pairings
  trimmed to 3 per side), plus `week_cards`, `games`, `weeks` and a Q6 `signals` sample.
- **Tests:**
  - `npm test`: 30 pass. `card.test.ts` (real fixtures, not-projected shape, failure
    modes). `db.test.ts` (mocked fetch: headers, params, every query on real response
    shapes, HTTP errors, shape drift, row cap, param injection).
    `read-boundary.test.ts`.
  - `npm run test:live` (never in `npm test`) runs the production `lib/db.ts` against
    the live views. Every week row; every game and card of every carded week (all 16
    cards parse); a full Q6 set (80 efficiency rows + availability, market,
    environment; 0 player rows); a 2020 game's Q6 (efficiency only). 4 pass.
- **Shown capable of failing:**

  | Deliberate break | Result |
  |---|---|
  | Fixture `projection.spread_home` changed to a string | 3 card-parse tests failed |
  | Row-cap guard `>=` → `>` | "refuses a result that hit the row cap" failed |
  | `checkTeam(home)` removed | "rejects a bad team" failed |
  | `fetch(process.env.NEXT_PUBLIC_SUPABASE_URL)` added to `lib/format.ts` | The fetch, process.env and NEXT_PUBLIC_ rules failed. **The SUPABASE_ rule passed: a real gap.** |
  | Live: `weeks.first_gameday` typed as number | Q1, Q2–Q5, Q6 failed (all start from `listWeeks`) |
  | Live: card term `sample_n` typed as string (alone, so it isn't masked by the above) | Q2–Q5 failed, naming `2026_03_ARI_SF` and each term path |

  **About the SUPABASE_ gap:**
  - `\bSUPABASE_` misses `NEXT_PUBLIC_SUPABASE_URL`, because the `_` before `S` is a
    word character, so there's no word boundary.
  - Fixed by dropping the `\b`. Re-run against the same break, the rule now fails
    correctly.

  Every break was reverted. The clean runs pass: 30/30 unit, 4/4 live, `tsc` clean,
  `next build` passes.

### Step 2 results (2026-09-24)

- **Scaffold:** `web/` holds `package.json` (exact pins per §1, `engines.node 24.x`),
  `tsconfig.json`, `next.config.ts`, `app/tokens.css`, `app/base.css`, `app/layout.tsx`,
  `app/page.tsx` (placeholder), `components/status-line.tsx` and `lib/format.ts`.
  - `tokens.css` holds every color, type size and spacing value. `base.css` uses tokens
    only.
  - `next build` rewrote `tsconfig.json`: `jsx: react-jsx`, `allowJs`, and the
    `.next/dev/types` include.
- **The status line shows no figures until step 4.** Placeholder numbers could be
  mistaken for the result.
- **Checks:**
  - `npm run build` and `tsc --noEmit` pass.
  - The layout check is `web/scripts/check-layout.mjs` (reused by steps 5–6). It drives
    the installed Chrome headless through `playwright-core` 1.63.0, a pinned dev
    dependency with no browser download. At 375px and 1280px, light and dark, it
    requires no horizontal page scroll **and** that the styling is live: body background
    equal to the token, status line 12px, title 20px, both Plex fonts loaded.
- **Shown capable of failing:** three deliberately broken states (the script's
  `--break=` modes), each failing correctly with exit 1.

  | Broken state | Result |
  |---|---|
  | All stylesheets blocked | FAIL at every width and scheme (`styled=false`) |
  | A 600px element injected into `<main>` | FAIL at 375px (scrollWidth 616). PASS at 1280px, where it genuinely fits. |
  | Font files blocked | FAIL everywhere (`styled=false`) |

  The clean build passes all four width/scheme cases.

### Step 1 results (2026-09-24)

- **Versions, rendering, key type, hosting:** see §1 ("Pinned versions" and what follows
  it).
- **The Odds API terms** (https://the-odds-api.com/terms-and-conditions.html, "Last
  updated: 31 August 2026"; quoted verbatim in `docs/sources.md` → Odds → Display terms):
  - **Displaying derived values is explicitly permitted.** "Calculating and displaying
    values you derive from our data" and "Displaying our data in a UI, website, or mobile
    app, including for commercial use" are both listed.
    - The consensus spread/total, movement, book range, straddle, and implied totals
      shown on the game view fall under these.
    - Attribution isn't required.
  - **Decided 2026-09-24: (a), leave as is.** The reasoning and the conditions for
    revisiting it are in `docs/sources.md` → Odds → Display terms. The question as it was
    put to the user: the `web` schema from 0026.
    - It's a PostgREST API that returns the market-sector signals in bulk to anyone
      holding the anon key.
    - The key stays server-only, but §2d treats it as public.
    - The restriction reads: "offering our data through your own API, data feed,
      downloadable files, or any other format intended to serve as a source of raw data
      for others".
    - The key's purpose is the web app's own server-side reads, not serving others. But
      in practice it's reachable as an API.
    - Narrowing anon's `signals` policy wouldn't remove this on its own. Every card
      embeds the full market block (`card.market.signals`, the edge block's lines), and
      `web.cards` is just as readable in bulk.
    - Options, for the user's call:
      - (a) Leave as is.
      - (b) No REST surface at all. The web server connects to Postgres directly (the
        Supavisor pooler) as a new login role with `SELECT` only on `web`. The Data API
        is then disabled, and 0026's anon grants are revoked in a new migration.
        - Nothing is reachable with a browser-type key.
        - Costs a Postgres client dependency, and a DB credential that must stay
          server-side (unlike the anon key, a leak of it isn't "public by design").
        - Changes §1's data-access row and §2c's query form. Same views, SQL instead of
          PostgREST.
      - (c) Email The Odds API, per the terms' own "If you are unsure whether your use
        case is permitted, please contact us."
  - **Responsible-gambling clause:** applies only "if the Service is used to promote
    bookmakers or gambling services". The app promotes none and names no books.

## 8. Decisions (2026-09-24)

1. **Edge in the week view: omit.** Lines sit side by side; the edge appears with its
   tag only on the game view.
2. **Player-level availability rows: excluded from v1** (the RLS policy enforces it).
   Decide with P7 whether L3 may read `players` identity columns.
3. **`injuries_backup_pre_changelog`: 0026 closes it now. Don't drop it yet.**
   - Drop it in a later migration, after Sunday 2026-09-27's slate runs clean through
     the changelog path.
   - At 191/500 MB, its 32 MB isn't urgent.
4. **Scope rewrite confirmed.** Player view, signal cross-reference, and auditor badges
   leave P6. `KICKOFF.md`'s P6 rows are updated.
5. **No ranks in v1, but the column is reserved** (§4 table treatment). "Efficiency fills
   `league_pct`" is the top item in `docs/phases/P2.md`'s pending amendments. It's not
   in P6.
6. **Pre-2026 games: linked, marked "no card".**
7. **Live grades: unexposed in v1.** Revisit at ≥ `CLV_MIN_N` graded games, and only ever
   with verdicts and CIs attached, never a bare win rate.
8. **Domain: `*.vercel.app`.** $0 is hard.
9. **Egress baseline:** check the Supabase Usage page before step 9 and record the
   pipeline's current monthly egress in §1.

## Files to create

```
db/migrations/0026_web_read_access.sql
scripts/verify_anon_access.py
web/                                  # Next.js app
  app/tokens.css, app/layout.tsx, app/page.tsx
  app/[season]/[week]/page.tsx, app/game/[gameId]/page.tsx
  app/method/page.tsx, app/sources/page.tsx
  lib/db.ts, lib/card.ts, lib/format.ts, lib/signal-labels.ts
  scripts/sync-content.mjs            # prebuild copy + report parse
  tests/fixtures/card_2026_03_ATL_GB.json, tests/*.test.ts
```

## Done when
- Anon access is exactly §2d, verified by `scripts/verify_anon_access.py`.
- The week and game views render every state in the live data: projected, locked,
  provisional, not projected, and no card.
- The honesty suite passes, and every edge carries its validation tag.
- Deployed on Vercel Hobby with the key absent from client output.
- `docs/architecture.md`, CLAUDE.md, and this file updated.
