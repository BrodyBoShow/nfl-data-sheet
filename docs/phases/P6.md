# P6 — Data sheet UI

**Status:** not started (blocked on P5)

## Scope
- Scaffold `/web`: Next.js App Router + TypeScript, deployed to Vercel.
- Reads Supabase only via views (no external calls, no direct writes from the UI).
- Game view, player view, signal cross-reference and filters.
- Matchup cards showing sample sizes and data-freshness/staleness badges (from the
  auditor's per-domain status).

## Files to create
```
web/                              # Next.js app
db/migrations/00NN_ui_views.sql   # read-optimized views for the UI
docs/phases/P6.md (this file, updated with actual routes/components once built)
```

## Done when
- User can filter games and players.
- User can cross-reference any signal.
- Matchup cards show sample sizes and staleness badges sourced from the auditor.
- Deployed and reachable on Vercel free tier.
