# P8 — Live and narrator

**Status:** not started (blocked on P7)

## Scope
- Live game collector: ESPN scoreboard/game summary (unofficial). Stores live_games,
  live_box, espn_lines. Polls as one looping job per game window, not a new job every
  few minutes.
- Dispatcher tuning: refine the idle-window logic built in P1 against real usage data
  from prior phases; confirm GitHub Actions minutes stay within free tier.
- **Optional, small-cost exceptions only:**
  - LLM news parsing (Haiku batch, changed items only) as an upgrade to Phase 7's
    rule-based tagging.
  - Matchup narrator: on-demand prose generated from one card's signals, cached per game
    per day, cannot introduce numbers not already on the card. Requires
    `ANTHROPIC_API_KEY`.

## Files to create
```
pipeline/collectors/live.py
pipeline/orchestration/dispatcher.py   # tuned, not new
pipeline/synthesis/narrator.py         # optional
tests/fixtures/espn_scoreboard_sample.json
tests/fixtures/espn_boxscore_sample.json
tests/test_live.py
tests/test_narrator.py                 # optional
```

## Done when
- Live scores update during an actual game window.
- GitHub Actions minutes and (if used) Odds/Anthropic credits reviewed and confirmed
  within budget.
