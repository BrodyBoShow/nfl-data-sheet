# P4 — Market and environment

**Status:** in progress. Collectors done: odds done; weather done and verified live
(2026-09-23); stadiums loaded (41 rows). **Environment analyst built 2026-09-23** (see
"Environment analyst" below; awaiting the live spot-check). **Remaining: Market analyst**
(not started).

Odds collector done: scheduling
(`pipeline/collectors/odds_schedule.py`, `db/migrations/0012_odds_snapshot_targets.sql`)
computes the weekly snapshot-target calendar (tue_opener, sat_market_movement, sun_early,
sun_late, thu_pre_tnf, mon_pre_mnf) from real `games` kickoff times and gates firing on a
per-target time window plus weekly (21 credits) and monthly (500 credits) budget
ceilings. fetch/validate/store (`pipeline/collectors/odds.py`,
`db/migrations/0013_odds_snapshots.sql`, `db/migrations/0014_odds_consensus.sql`) stores
every bookmaker per event per poll (not one reference book) plus a derived per-poll
consensus (median spread/total point + cross-book disagreement range), built and tested
against a live-verified response (`docs/sources.md`'s Odds entry, live-called
2026-09-18). Registered in the dispatcher and `pipeline.run`'s CLI job list, with its own
schedule-aware auditor check (`auditor.check_odds_targets`) instead of a generic
freshness check.

Two fixes since first standing this up, both surfaced by the first live captures:
- `pipeline/core/schedule.py`'s `resolve_season_week` and `odds.py`'s `_match_game_id`
  both took `.date()` on a raw UTC datetime instead of converting to ET first, so any
  prime-time kickoff (TNF/SNF/MNF, at/after ~8pm ET) silently resolved against the wrong
  week -- confirmed live on the actual 2026 week 2 MNF game. Fixed via a shared
  `pipeline.core.schedule.to_gameday`; `scripts/inspect_injuries_week_labeling.py`
  confirms no `injuries` rows were actually mislabeled by the same bug (no observed
  `as_of` ever fell in the danger window).
- `tue_opener`'s window was ~3.5 days wide (Tue 10am ET to Sat midnight) and fired
  Friday evening in week 2 -- not an opening line. Narrowed to close Wednesday 9am ET;
  a missed opener is accepted as better than a mislabeled one. The 2-hour/20-minute
  pre-kickoff windows (thu_pre_tnf/mon_pre_mnf/sun_late) proved too narrow the same
  week (thu_pre_tnf was missed entirely) and widened to a shared 4-hour
  `_PRE_KICKOFF_WINDOW`.

Weather collector **done and verified live** (build steps 1–4 below): `stadiums` collector
(`pipeline/collectors/stadiums.py`, 41 rows loaded 2026-09-23 from
`reference/stadiums.csv`), kickoff-relative schedule (`weather_schedule.py`), Open-Meteo
collector (`weather.py`), auditor checks (`check_venue_problems`,
`check_weather_targets`), migrations 0018–0020. Pre-run check 2026-09-23 via the
collector's own `resolve_venue`: weeks 3–4 → 27 fetch, 5 `fixed_roof`, 0 other skips;
the only name-guard mismatch in the rest of 2026 is `2026_05_PHI_JAX`, as expected.
First live run 2026-09-23 17:12 UTC (`2026_03_ATL_GB`): t48 marked `missed`
(`deadline_passed`, window closed before the first run), t36 `captured` with 5 rows
(hours H..H+4, `lead_hours` 31.05 = kickoff − as_of, `model_regime_break` false);
Open-Meteo snapped the requested Lambeau field centroid to a grid cell ~1.1 km away
(consistent with HRRR's ~3 km grid); remaining targets pending. The
`model_regime_break = true` path is so far covered only by tests — first live t48
capture expected with the week 3 Sunday slate.
Market analyst is **not started**.

### Environment analyst (built 2026-09-23)
`pipeline/analysts/environment.py`. The full signal definitions are in `docs/signals.md`,
"Environment sector". Decisions:
- **Per-game window, not `ctx.week`**: every game with `now − 24h < kickoff ≤ now + 7d`.
  Rows carry the game's own season/week, and stale cleanup is scoped to the window's
  `game_id`s (`_delete_stale_signals` override). Reason: weather belongs to a game, and a
  Thursday game's snapshots land before nflreadpy's week advances.
- **Headline snapshot** = latest capture before kickoff. `lead_hours` is exposed as
  `weather_lead_hours`, not folded into `stability`.
- **`weather_status` separates dome / missed / never tracked**: a dome comes from
  `stadiums` alone, a miss from targets. A stored 3 after kickoff is stale, not "awaiting"
  (documented in signals.md).
- **Speed-only is the base shape** (`wind_speed_mph` + `wind_direction_mode`). The
  along-field/crosswind split is extra rows for qualifying (≥ 8 mph) hours at venues with
  a field bearing.
- **International confidence** (the open question above): resolved by deriving it from
  venue coords, with no new column. `weather_forecast_domain` projects the stadium onto
  NOAA's HRRR CONUS grid (`docs/sources.md`); every US venue is inside and every
  international one outside.
- **Timezone**: new `stadiums.tz` (migration `0021_stadiums_tz.sql`, CSV column,
  collector-validated). A team's home tz comes from its modal home stadium. Primary
  `tz_shift_hours` is wrapped to ±12 (Melbourne: +17 → −7), and the raw difference is
  kept as `tz_offset_diff_raw_hours`. International magnitudes are never clipped.
- **Altitude**: `venue_elevation_m` from the snapshot's `grid_elevation_m` (Open-Meteo 90 m
  DEM), so it only exists for fetched games. **Deferred:** a `stadiums` elevation column
  for full coverage. Every high-altitude venue (DEN00, MEX00) is open-air, so they're
  always fetched.
- **Venue guard shared**: `GameVenue`/`Stadium`/`VenueDecision`/`resolve_venue` moved from
  `weather.py` to `pipeline/core/venue.py` (re-exported from `weather.py`, with no
  behavior change). The analyst applies the collector's exact rule instead of a copy.
- **Not built:** forecast-movement signals (t36 onward), and a travel origin from the
  previous game's venue (travel is always measured from the home venue).
- **Live preview 2026-09-23 (week 3)** via `scripts/inspect_environment.py` matched the
  expected results:
  - `2026_03_ATL_GB` was status 1, wind mode 1.
  - Fixed-roof games were status 2.
  - At RIO00, both teams showed travel.
  - Arizona vs. Pacific resolved to a 0h shift in September.
- **Watch item: 13 of 16 week-3 games were status 3** at preview time. That's expected: the
  Sunday games were beyond the collector's 54h target horizon, so they had no target rows
  yet. For a 1pm ET Sunday kickoff, t48 opens Friday ~1pm ET, so these should reach
  status 1 by Friday evening.
  - **If Sunday games are still status 3 on Saturday**, the weather collector isn't
    creating targets as games come into range (`weather_schedule.ensure_targets` /
    `upcoming_games`). The whole weekend slate would then get no weather at all.
  - Check `weather_snapshot_targets` for the Sunday `game_id`s before digging into the
    analyst: missing rows is a collector problem; rows that are `missed` or pending with no
    capture points at dispatcher tick gaps.

### Weather design decisions (agreed 2026-09-22, before build)
Open-Meteo verified live 2026-09-23 (`docs/sources.md`). Build order: (1) source
verification — done; (2) OSM field-geometry script + hand-reviewed `reference/stadiums.csv`;
(3) migrations; (4) `stadiums` collector, weather schedule, `weather` collector + auditor
check; (5) docs.
- **`stadiums` keyed on nflverse `stadium_id`** (not team — MetLife/SoFi are one place
  each). 41 rows: the 38 IDs on the 2026 schedule plus past international FRA00, GER00,
  SAO00. Columns: `lat`, `lon`, `roof_type` (fixed/retractable/open), `field_bearing`
  (0–179°, field is symmetric), `osm_way_id`, `known_names text[]`, source note. Roof
  *state* and surface stay per-game on `games` (retractables change per game, surfaces get
  replaced). Data lives in a checked-in CSV loaded by a hash-diffed collector, not in a
  migration (aliases get added over time; applied migrations can't be edited).
- **Coords + bearing from OpenStreetMap**: the field polygon's centroid and longest-edge
  bearing, cited by OSM way ID, picked by hand per stadium (105–125 m long, inside the
  stadium; Lambeau test: way 145797338, 113 m, ~180°, while a nearby practice field also
  matched the tag). Two IDs for one venue (GER00/MUN01 = Allianz Arena) cite the same
  way, so they can't drift. No usable polygon → `field_bearing` null.
- **Name guard, not a hardcoded fix**: each run checks `games.stadium = ANY(known_names)`
  for the game's `stadium_id`; on mismatch (or unknown `stadium_id`) don't fetch, record
  the game under `meta.unresolved`, auditor alerts. Checked 2018–2026: 182 games differ
  from their ID's most common name, but all except one are sponsor renames/spellings
  (incl. every 2026 HOU game reading the retired "Reliant Stadium"); the one real
  mislabel is `2026_05_PHI_JAX` (JAX00 but Tottenham Hotspur Stadium). A single-name
  guard would have thrown 48 false alerts in 2026 — hence the alias list.
- **Roof rule**: `roof_type=fixed` → skip; `retractable` → skip only when
  `games.roof='closed'`, else fetch (nflverse leaves it null pre-game) and label signals
  "if roof open"; `open` → always fetch, alert if `games.roof` claims dome/closed.
- **Wind is an exterior estimate**: columns named `wind_speed_10m_mph`,
  `wind_gusts_10m_mph`, `wind_direction_10m_deg`; Environment analyst frames wind signals
  as a relative indicator, never field-level wind. `field_bearing` lets it split
  along-field vs. crosswind components of that exterior estimate.
- **Snapshot schedule, 7 per game, relative to kickoff, weighted late**: T-48h, T-36h,
  T-24h, T-18h, T-12h, then two wide late buckets — `t6` (T-6h to T-2h) and `t2` (T-2h
  to kickoff+1h). Earlier snapshots were cut as mostly model noise. See "Late targets
  collapsed" below for why the late end isn't T-6/4/2/1/0. Target-window gating like `odds_schedule` (GitHub drops cron
  runs; a missed target stays missed); each row stores actual `lead_hours`. A target with
  no hourly data yet (HTTP 400, or nulls in the window) is skipped, never filled.
  Replaces "weather 3×/day from Wed / hourly Sun" in `docs/architecture.md`.
- **T-48h is kept but flagged, not dropped** (decided 2026-09-23). Live verification
  showed T-48h lands past HRRR's reach (HRRR's extended runs go 48h ahead every 6h, so
  with run latency its live coverage ends ~42–46h out), so T-48h is GFS while T-36h onward
  is HRRR at US venues. Each row stores its schedule `target`, and T-48h rows carry
  `model_regime_break = true`. Movement comparisons ("forecast changed") start at T-36h and
  never diff T-48h → T-36h; the Environment analyst reads T-48h only as a standalone
  early look. Why keep it: it's the only forecast that exists when Sunday cards are
  drafted Friday afternoon (≈T-48h for 1pm ET kickoffs, per the architecture calendar),
  and the flag costs one column. It's flagged by target rather than inferred per venue,
  because at international venues `best_match` uses different models whose switch
  points haven't been verified — excluding the earliest snapshot everywhere is the
  conservative uniform rule.
- **Late targets collapsed to `t6`/`t2`** (decided 2026-09-23, before the first weather
  run). Originally T-6h, T-4h, T-2h, T-1h, T-0 with 2h/2h/1h/1h/1h windows. Measured
  dispatcher tick spacing (scheduled ticks in `agent_runs`, 2026-09-21..23) is 4.5–5.5h,
  max gap 6h46m, despite the 15-minute cron — so a 1-hour window is caught ~20–25% of
  the time and only 1–2 ticks land in the whole T-6h..kickoff span. Five narrow targets
  would have yielded about one snapshot per game filed under whichever label happened to
  be open, plus a row of misses. A missed row is worse than a wider bucket: every
  snapshot stores its true `lead_hours`, so the bucket label loses no precision. The two
  buckets tile T-6h..kickoff+1h with no gap, so any tick in that span captures. `t6`'s
  4h width matches odds' `_PRE_KICKOFF_WINDOW`. T-24/18/12 windows (6h) were left alone.
  No migration needed: `t6`/`t2` were already in 0019's `target_id` CHECK; `t4`/`t1`/`t0`
  are simply never generated (the CHECK still allows them — harmless).
- **Known option, not built — real late-window resolution**: if T-1h-level precision is
  ever needed, the fix is a reliable trigger, not narrower windows: Supabase `pg_cron` +
  `pg_net` calling GitHub's `workflow_dispatch` API ($0 on the free tier), with a GitHub
  token stored in Supabase Vault. Skipped 2026-09-23 as more moving parts (extension, a
  stored token, a second scheduler) than T-1h precision is worth today.
- **Null `field_bearing` is a first-class path**: 20 of 41 stadiums have no bearing
  (OSM maps no field polygon, only a soccer pitch, or fixed roof). The Environment
  analyst must treat "wind speed only, no along-field/crosswind split" as a normal,
  designed output for those venues — its own signal shape, spot-checked like the split
  path — not an edge case that leaks nulls into downstream signals.
- **Low wind → speed only, no direction component** (decided 2026-09-23). Below
  **8 mph** sustained (`wind_speed_10m_mph`, per forecast hour), the Environment
  analyst reports wind speed only and computes no along-field/crosswind split, even
  where `field_bearing` exists. Evidence: the first live capture (`2026_03_ATL_GB`
  t36, 2–5 mph) swung from 84° to 308° across five hours — direction at that speed
  is noise. Why 8 mph: (a) light winds carry no stable direction (US aviation weather
  practice reports direction as "variable" at ~6 kt ≈ 7 mph and below), so 8 sits just
  above the band where direction is unreliable in observations, let alone a 30h-out
  forecast; (b) wind is rarely treated as a passing/kicking factor below ~10 mph, so
  the split is dropped only where it couldn't matter anyway; (c) this is the *exterior*
  10 m estimate — the bowl shelters the field further, so the true field-level wind is
  lower still. Gusts don't lift an hour over the threshold (a 9 mph gust on 3 mph
  sustained is still directionless air). Revisit the number once the grader has a
  season of snapshots vs. outcomes. Combined with the 20 null-bearing venues, **"speed
  only" is the common case, not the exception** — so the speed-only signal shape is
  the primary output and the directional split is the add-on. (How often games clear
  8 mph is not yet measured; check it once a few weeks of snapshots exist.)
- **International venues use unverified Open-Meteo models**: at non-US venues (e.g.
  RIO00, LON02 in 2026 weeks 3–4) `best_match` picks models whose behavior and switch
  points haven't been verified (`docs/sources.md` only verified HRRR/GFS at US venues).
  The Environment analyst must mark signals from those games as lower confidence and
  not treat them identically to US forecasts. Not yet flagged at storage:
  `weather_snapshots` has no per-row confidence column. When the analyst is built,
  decide between deriving it from venue (stored coords outside the HRRR/CONUS domain)
  or adding a flag column via a new migration — don't hardcode a stadium_id list.
- **Stored window**: hours H..H+4 for a kickoff in hour H (Open-Meteo precip/gusts are
  preceding-hour aggregates, so H+4 covers the game's last hour). Projection: ~215
  fetched games/season × 7 snapshots × 5 rows ≈ 7,525 rows ≈ 2 MB/season; ~1,505
  requests/season at 1.0 call each (≤10 variables).

## Scope
- Odds collector: The Odds API free tier + ESPN embedded lines as free fill-in. Append-
  only `odds_snapshots`. Credit usage logged and budgeted (~120 credits/month, see
  `docs/architecture.md` calendar).
- Weather collector: Open-Meteo (no key) + stadium coords/roof/surface. Append-only
  `weather_snapshots`, outdoor games only.
- Market analyst: open vs. current line, movement velocity, implied team totals, key-
  number crossings. No "sharp money" claims.
- Environment analyst: wind/precip flags for passing/kicking, dome/outdoor, surface,
  altitude, rest differential, travel distance, timezone crossings.
- Source verification for Odds API, ESPN embedded lines, Open-Meteo per
  `docs/sources.md`.

## Files to create
```
db/migrations/0012_odds_snapshot_targets.sql  # DONE -- scheduling state
db/migrations/0013_odds_snapshots.sql         # DONE -- per (event, bookmaker, poll)
db/migrations/0014_odds_consensus.sql         # DONE -- not in the original file list;
                                               # derived median/disagreement per (event, poll)
db/migrations/0018_stadiums.sql               # DONE -- keyed on stadium_id; roof state and
                                               # surface stay per-game on games
db/migrations/0019_weather_snapshot_targets.sql  # DONE -- not in the original file list;
                                               # kickoff-relative scheduling state
db/migrations/0020_weather_snapshots.sql      # DONE -- append-only
pipeline/collectors/odds.py                   # DONE
pipeline/collectors/odds_schedule.py          # DONE -- not in the original file list;
                                               # split out of odds.py so the scheduling
                                               # logic could be built/tested before the
                                               # source was verified (see status above)
pipeline/collectors/stadiums.py               # DONE -- not in the original file list;
                                               # loads reference/stadiums.csv (hash-diffed)
pipeline/collectors/weather_schedule.py       # DONE -- not in the original file list;
                                               # scheduling half, like odds_schedule.py
pipeline/collectors/weather.py                # DONE
reference/stadiums.csv                        # DONE -- hand-reviewed coords/roof/bearing
scripts/osm_stadium_fields.py                 # DONE -- OSM field-geometry lookup
scripts/build_stadiums_csv.py                 # DONE -- assembles reference/stadiums.csv
db/migrations/0021_stadiums_tz.sql            # DONE -- not in the original file list;
                                               # IANA tz per venue for timezone crossings
pipeline/core/venue.py                        # DONE -- not in the original file list; venue
                                               # guard shared by weather.py + environment.py
pipeline/analysts/market.py
pipeline/analysts/environment.py              # DONE
scripts/inspect_environment.py                # DONE -- not in the original file list;
                                               # read-only preview via compute()
scripts/inspect_injuries_week_labeling.py     # DONE -- not in the original file list;
                                               # one-off audit for the resolve_season_week
                                               # timezone fix's blast radius
tests/fixtures/odds_api_sample.json           # DONE
tests/fixtures/open_meteo_forecast_sample.json  # DONE
tests/test_odds.py                            # DONE
tests/test_odds_schedule.py                   # DONE -- not in the original file list
tests/test_stadiums.py                        # DONE -- not in the original file list
tests/test_weather_schedule.py                # DONE -- not in the original file list
tests/test_weather.py                         # DONE
tests/test_market.py
tests/test_environment.py                     # DONE
```

## Done when
- Credit usage logged per run and confirmed under monthly budget.
- Weather signals written only for outdoor games.
- Market and environment signals written and spot-checked.
