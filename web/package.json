{
  "name": "nfl-data-sheet-web",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "sync-content": "node scripts/sync-content.mjs",
    "predev": "npm run sync-content",
    "dev": "next dev",
    "prebuild": "npm run sync-content",
    "build": "next build",
    "start": "next start",
    "pretypecheck": "npm run sync-content",
    "typecheck": "tsc --noEmit",
    "pretest": "npm run sync-content",
    "test": "vitest run",
    "test:live": "vitest run --config vitest.live.config.ts"
  },
  "dependencies": {
    "next": "16.3.6",
    "react": "19.3.0",
    "react-dom": "19.3.0",
    "zod": "4.6.5"
  },
  "devDependencies": {
    "@types/node": "24.13.6",
    "@types/react": "19.3.0",
    "@types/react-dom": "19.3.0",
    "playwright-core": "1.63.0",
    "typescript": "6.0.3",
    "vitest": "5.0.1"
  }
}
